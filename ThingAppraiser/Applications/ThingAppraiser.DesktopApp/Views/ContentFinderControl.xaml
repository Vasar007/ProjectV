<UserControl x:Class="ThingAppraiser.DesktopApp.Views.ContentFinderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ThingAppraiser.DesktopApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <ScrollViewer>
        <StackPanel>
            <Button Width="256"
                Margin="0 32 0 0">
                <!--Command="{Binding Path=OpenFileDialogCommand}"
                CommandParameter="{Binding Path=DataContext,
                                   RelativeSource={RelativeSource Mode=FindAncestor,
                                                   AncestorType={x:Type Window}}}"-->
                Choose directory to find content
            </Button>

            
        </StackPanel>
    </ScrollViewer>
</UserControl>
